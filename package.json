{
  "name": "downview",
  "version": "1.0.0",
  "description": "Downview application",
  "scripts": {
    "test": "html-validator --file index.html --verbose"
  },
  "devDependencies": {
    "html-validator-cli": "^7.0.1",
    "broken-link-checker": "^0.7.8"
  }
}
